/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },
  // Enable if using Supabase auth redirects:
  // async redirects() { return [] },
}

export default nextConfig
